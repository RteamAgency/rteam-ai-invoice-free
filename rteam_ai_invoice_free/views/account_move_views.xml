<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="account_move_form_rteam_ai_invoice" model="ir.ui.view">
        <field name="name">account.move.form.rteam.ai.invoice</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_post']" position="before">
                <button
                    name="action_rteam_ai_extract"
                    string="Extract from PDF/JPG"
                    type="object"
                    class="btn-secondary"
                    invisible="move_type not in ('in_invoice', 'in_refund') or state != 'draft'"
                    groups="account.group_account_invoice"
                />
            </xpath>
        </field>
    </record>
</odoo>
